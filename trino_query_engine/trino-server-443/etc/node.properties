# The name of your environment (anything you want, usually 'production' or 'development')
node.environment=production

# Unique ID for this Trino node
node.id=trino-coordinator

# Directory where Trino stores temporary data
node.data-dir=/tmp/trino-data
